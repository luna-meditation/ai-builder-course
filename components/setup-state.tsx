import { Database, FileCode2, TerminalSquare } from "lucide-react";
import { Logo } from "@/components/ui/logo";

export function SetupState() {
  return <main className="grid min-h-screen place-items-center px-5 py-12"><div className="card w-full max-w-2xl p-6 sm:p-9"><Logo /><div className="eyebrow mt-10">Первый запуск</div><h1 className="mt-3 text-3xl font-bold tracking-tight">Подключите Supabase, чтобы запустить платформу</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[var(--muted)]">Интерфейс готов, но сервер не видит переменные окружения. Это безопасное состояние: приложение не подменяет реальные данные демонстрационными.</p><div className="mt-7 grid gap-3 sm:grid-cols-3">{[{ icon: FileCode2, title: "1. Переменные", text: "Скопируйте .env.example в .env.local" }, { icon: Database, title: "2. База", text: "Примените миграции и development seed" }, { icon: TerminalSquare, title: "3. Запуск", text: "Перезапустите локальный сервер" }].map(({ icon: Icon, title, text }) => <div key={title} className="rounded-2xl border border-[var(--border)] bg-white/[.025] p-4"><Icon className="size-5 text-[#a89eff]" /><p className="mt-3 text-sm font-semibold">{title}</p><p className="mt-1 text-xs leading-5 text-[var(--muted)]">{text}</p></div>)}</div><p className="mt-6 text-xs text-[var(--muted)]">Точные команды и проверка настройки описаны в README.</p></div></main>;
}
